openai>=1.0.0
numpy>=1.24.0
tqdm>=4.65.0
python-dotenv>=1.0.0
# Use a version that works with stdlib sqlite3 without requiring load_extension
sqlite-vec>=0.1.6
# NOTE: This project assumes a Python build where sqlite3 supports loadable extensions.
